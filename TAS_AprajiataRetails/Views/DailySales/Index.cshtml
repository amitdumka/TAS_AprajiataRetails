@model IEnumerable<TAS_AprajiataRetails.Models.Data.DailySale>

@{
    ViewBag.Title = "Index";
}

<h2>Daily Sale</h2>
<br />
<div>
    <h4 class="text-success">Today Sale  : @ViewBag.TodaySale         Manual Sale: @ViewBag.ManualSale        Monthly Sale: @ViewBag.MonthlySale</h4>
    <h4 class="text-danger">Total Dues  : @ViewBag.DuesAmount         Cash-In-Hand: @ViewBag.CashInHand</h4>
    <div class="column"></div>
    <div class="column"></div>


</div>



<p>
    @Html.ActionLink("Create New ", "Create", new { id = 0 }, new { data_modal = "" })
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.SaleDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InvNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PayMode)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.CashAmount)
        </th>*@
        @*<th>
            @Html.DisplayNameFor(model => model.IsDue)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.IsManualBill)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsSaleReturn)
        </th>
        @*    <th>
            @Html.DisplayNameFor(model => model.Remarks)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Salesman.SalesmanName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.SaleDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PayMode)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.CashAmount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsDue)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.IsManualBill)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.IsSaleReturn)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salesman.SalesmanName)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Remarks)
                </td>*@
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.DailySaleId }, new { data_modal = "" }) |*@
                @Html.ActionLink("Details", "Details", new { id = item.DailySaleId }, new { data_modal = "" }) |
                @*@Html.ActionLink("Delete", "Delete", new { id = item.DailySaleId }, new { data_modal = "" })*@
            </td>
        </tr>

    }

</table>
<!-- modal placeholder-->
<div id='myModal' class='modal fade in'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContent'></div>
        </div>
    </div>
</div>
@section Scripts {

    <script type="text/javascript">

        $(function () {
            $.ajaxSetup({ cache: false });
            $("a[data-modal]").on("click", function (e) {
                $('#myModalContent').load(this.href, function () {
                    $('#myModal').modal({
                        keyboard: true
                    }, 'show');
                });
                return false;
            });
        });


        $(function () {
            $('a.dialog').click(function () {
                var url = $(this).attr('href');
                var dialog = $('<div></div>').appendTo('body');
                dialog.load(url, {},
                    function (responseText, textStatus, XMLHttpRequest) {
                        dialog.dialog({
                            close: function (event, ui) {
                                dialog.remove();
                            }
                        });
                    });
                return false;
            });
        });
    </script>
}
